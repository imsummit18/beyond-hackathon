import { OctagonAlert } from 'lucide-react';
import React from 'react';

const OctagonIcon = () => {
  return <OctagonAlert size={24} fill='#F0B441' color='#fff' strokeWidth={2} />;
};

export default OctagonIcon;
